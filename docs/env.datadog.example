# ===========================================
# Datadog Cloud Account Configuration
# ===========================================
# Copy this file to .env.datadog in the root directory and fill in your credentials
# from your Datadog account at https://app.datadoghq.com
#
# To get your keys:
# 1. Log into Datadog → Organization Settings → API Keys (for DD_API_KEY)
# 2. Log into Datadog → Organization Settings → Application Keys (for DD_APP_KEY)
# ===========================================

# Required: Your Datadog API Key
DD_API_KEY=<your-api-key-from-datadog-account>

# Required: Your Datadog Application Key (needed for API access)
DD_APP_KEY=<your-app-key-from-datadog-account>

# Datadog Site (choose your region)
# Options: datadoghq.com (US1), us3.datadoghq.com (US3), us5.datadoghq.com (US5),
#          datadoghq.eu (EU), ap1.datadoghq.com (AP1)
DD_SITE=us5.datadoghq.com

# Environment name (for filtering in Datadog)
DD_ENV=hackathon

# Service name (root service identifier)
DD_SERVICE=v-commerce

# ===========================================
# LLM Observability Settings (Agentless Mode)
# ===========================================
# Agentless mode sends data directly to Datadog cloud - no local agent required

DD_LLMOBS_ENABLED=1
DD_LLMOBS_AGENTLESS_ENABLED=1
DD_LLMOBS_ML_APP=v-commerce-llm

# ===========================================
# APM Tracing Settings
# ===========================================

DD_TRACE_ENABLED=1
DD_PROFILING_ENABLED=false
DD_LOGS_INJECTION=true

# Version tag (update with each release)
DD_VERSION=1.0.0

# ===========================================
# RUM (Real User Monitoring) Settings
# ===========================================
# To get RUM credentials:
# 1. Log into Datadog → UX Monitoring → RUM Applications → Create Application
# 2. Copy the applicationId and clientToken from the setup instructions

DD_RUM_APPLICATION_ID=<your-rum-application-id>
DD_RUM_CLIENT_TOKEN=<your-rum-client-token>
